@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section head {
    <link rel="stylesheet" href="@Url.Content("~/Content/Views/Atendimento/FinalizarAtendimento.css?" + DateTimeOffset.Now)" />
}

@section scripts {
    <script src="@Url.Content("~/Scripts/Views/PessoaFisica/Index.js?" + DateTimeOffset.Now)"></script>
}

<div class="cssTituloAtendimento">
    <h3>Autenticação do Atendimento - Nome Paciente: @ViewBag.NomePessoa </h3>
</div>

@*<div class="row lign-items-center">
    <div class="col-md-10">
        <form action="@Url.Action("ValidarToken", "Atendimento")" method="post">
            <input type="hidden" name="tokenViewModel.NumeroAtendimento" value="@ViewBag.CodigoAtendimento">
            <div class="form-group">
                <label> Código Token </label>
                <input type="text" name="tokenViewModel.Token" />
            </div>
            <input class="btn-info" type="submit" value="Finalizar Atendimento" />
        </form>
    </div>
</div>*@

<form id="frmInserirAtendimentoIndex_Container" action="@Url.Action("ValidarToken", "Atendimento")" method="post">
    <input type="hidden" name="tokenViewModel.NumeroAtendimento" value="@ViewBag.CodigoAtendimento">
    <div id="divInserirAtendimentoIndex_Especialidade" class="cssInserirAtendimentoIndex_Box">
        <label class="cssInserirAtendimentoIndex_Box_Label"> Código Token </label>
        <input type="text" name="tokenViewModel.Token" />
    </div>
    <div class="cssInserirAtendimentoIndex_Box_Botoes">
        <button id="btnIndex_Salvar" type="submit" class="btn btn-primary" value="Finalizar Atendimento">Finalizar Atendimento</button>
    </div>
</form>